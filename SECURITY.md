# Security Policy

`rpg-blog` is a static site template. Please report vulnerabilities privately through GitHub's **Report a vulnerability** feature instead of opening a public issue.

The latest tagged release receives security fixes. Keep Node, npm dependencies, the base container, Docker Engine, and the host operating system updated.
