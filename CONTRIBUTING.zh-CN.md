# 贡献指南

这个仓库维护的是一个可复用博客模板，不只是某一个已经部署的网站。贡献应该帮助下面至少一个目标：

- 让第一次 fork 或使用模板的人更容易完成初始化
- 让默认部署行为更安全、更清楚
- 让个性化配置路径更明确
- 提升长期维护性

## 适合贡献的方向

- 部署加固
- 模板初始化流程
- 内容 schema 改进
- 功能开关行为
- setup、部署或升级文档
- 围绕共享模板行为的聚焦测试

## 需要谨慎避免的改动

- 引入强供应商绑定
- 给默认模板增加 CMS 依赖
- 加入只适合某一个个人博客的功能
- 做大范围视觉重设计，降低模板适配性
- 弱化生产环境认证或存储默认值

## 提交改动前

1. 保持改动范围聚焦。
2. 优先沿用仓库已有模式，不随意引入新抽象。
3. 行为变化时同步更新文档。
4. 共享行为变化时增加或调整测试。
5. 运行：

```bash
npm test
npm run build
```

## 模板专项评审标准

提出改动时，请说明：

- 这个改动帮助哪类 fork 用户
- 是否改变部署要求
- 是否改变内容结构
- 是否改变 setup 流程

## 最可能需要测试的区域

- `site.config.ts` 驱动的行为
- `scripts/` 里的 setup helper
- API 响应结构
- 功能开关
- 内容 schema 和草稿处理
