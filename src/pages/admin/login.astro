---
import BaseLayout from '../../layouts/BaseLayout.astro'
import AdminLogin from '../../components/vue/AdminLogin.vue'
import { t } from '../../lib/i18n'
import { getLocaleFromLocals } from '../../lib/locale'

const locale = getLocaleFromLocals(Astro.locals)
---

<BaseLayout title={t('admin.login', locale)}>
  <AdminLogin locale={locale} client:load />
</BaseLayout>
