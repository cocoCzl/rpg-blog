# 用户使用手册

这份文档解释 `rpg-blog` 首页里每个按钮和模块的作用，以及怎样把模板改成自己的博客、怎样写内容。

## 基本概念

`rpg-blog` 是一个静态博客模板。它没有后台编辑器，也不能直接在网页上写文章。你需要在本地编辑 Markdown/MDX 文件，预览确认后重新构建并部署。

项目把普通博客词汇包装成 RPG 公会手札语言：

- 手札条目：一篇博客文章。
- 手札库：所有文章列表，也就是博客归档页。
- 章节：文章分类，对应 `category`。
- 线索：文章标签，对应 `tags`。
- 角色档案：关于你自己的个人介绍页。
- 任务板：首页置顶文章区域。
- 最近存档：最近发布的文章列表。
- 道具栏工具箱：可选的资源、项目、工具或链接展示区。

## 首页按钮和模块

### 指令菜单

首页左侧的主要导航菜单，用来进入站点的核心页面。

- 手札库：进入 `/archive`，查看全部已发布文章。
- 章节地图：进入 `/chapters`，按分类查看文章。
- 线索：进入 `/tags`，按标签查看文章。
- 档案：进入 `/about`，查看作者介绍。

### 顶部主按钮

首页标题下方有两个主要按钮。

- 读取存档：打开当前置顶文章。如果没有置顶文章，会进入手札库。
- 档案：进入角色档案页，也就是关于页面。

### 角色档案槽

首页右侧的小档案卡片，展示作者名称、头像和简介。它的数据来自 `site.config.ts` 里的 `author` 配置。

### 任务板

任务板展示一篇置顶手札。默认取第一篇设置了 `featured: true` 的文章；如果没有设置置顶文章，就使用最新文章。

适合放：

- 你的第一篇介绍文章。
- 当前最想让访客阅读的文章。
- 项目说明、路线图、长期更新入口。

### 最近存档

最近存档展示最近的文章条目。每个存档卡片都可以点击进入文章详情。

存档条目里会显示：

- 发布时间。
- 章节，也就是 `category`。
- 前几个线索，也就是 `tags`。
- 标题和摘要。

### 最近存档标题栏按钮

最近存档标题右侧提供几个快捷入口：

- 章节地图：进入 `/chapters`。
- 公开链接：当你在 `site.config.ts` 里填写了 GitHub、Twitter 或网站链接时显示，点击会跳到首页下方的公开链接区域。
- 订阅源 RSS：进入 `/feed.xml`。这是给 RSS 阅读器使用的 XML 订阅源，普通浏览器打开时看起来像一段结构化文本，这是正常的。

### 公开链接

公开链接展示你配置的外部链接，比如 GitHub、Twitter 或个人网站。只有 `site.config.ts` 的 `social` 里填了链接，它才会显示。

### 道具栏工具箱

道具栏工具箱是一个可选的辅助展示模块，不是真实游戏背包，也不是文章列表。

它适合放：

- 你正在维护的项目。
- 常用工具或资源链接。
- 写作计划、路线图、作品入口。
- 你希望访客快速看到的个人亮点。

工具箱条目可以只是说明文字，也可以配置 `href` 变成可点击链接。是否显示由 `display.showToolbox` 控制。

安装向导会让你配置默认的两个工具箱条目。初始化后如果想继续增删或调整内容，可以编辑 `site.config.ts` 里的 `home.toolbox`。

## 怎么写文章

把 Markdown 或 MDX 文件放到 `src/content/posts/`。文件名会成为文章 URL 的一部分。

示例：

```md
---
title: "我的第一篇手札"
date: 2026-03-21
summary: "显示在存档板上的简短摘要。"
tags: ["写作", "记录"]
category: "启程"
featured: true
draft: false
---

在这里写正文。
```

常用字段：

- `title`：文章标题，必填。
- `date`：发布日期，必填。
- `summary`：摘要，必填，会显示在首页和列表页。
- `tags`：线索标签，可以写多个。
- `category`：章节分类。
- `cover`：文章封面路径。
- `updated`：可选的更新时间。
- `featured`：设为 `true` 时可进入首页任务板。
- `draft`：设为 `true` 时不会发布。

封面图片建议放到 `public/images/`，然后在文章里这样引用：

```md
cover: "/images/my-cover.png"
```

## 怎么修改站点信息

大部分个性化配置都在 `site.config.ts`。

常改字段：

- `siteUrl`：生产环境站点地址。
- `locale`：默认界面语言，`zh` 或 `en`。
- `title`：站点标题。
- `description`：站点简介。
- `author.name`：作者名称。
- `author.avatar`：作者头像路径。
- `author.bio`：作者简介。
- `social`：公开链接。
- `home.toolbox`：道具栏工具箱条目。
- `display.showToolbox`：是否显示道具栏工具箱。
- `postsPerPage`：列表页每页文章数量。

头像可以使用 PNG、JPG、WEBP 或 SVG。把图片放到 `public/images/` 后，路径可以写成：

```ts
avatar: '/images/avatar.png'
```

## 本地预览和发布

本地开发预览：

```bash
npm run dev
```

创建部署包：

```bash
npm run package:deploy
```

线上部署使用这个命令生成的 Docker 压缩包，不需要把博客长期放在自己电脑上跑。

部署细节见 [DEPLOYMENT.zh-CN.md](./DEPLOYMENT.zh-CN.md)。
